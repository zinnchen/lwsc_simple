import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.lwsc.kristian',
  appName: 'lwsc_simple',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
